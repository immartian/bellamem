"""Package-data templates for bellamem.

Files in this directory are bundled with the installed package via
`[tool.setuptools.package-data]` in pyproject.toml and read at runtime
through `importlib.resources.files("bellamem.templates")`. Currently
the only template is the Claude Code slash command definition.
"""
